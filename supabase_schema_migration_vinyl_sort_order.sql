-- 컬렉션 수동 정렬 (2026-07-28)
--
-- 배경: 사용자가 자신의 컬렉션(웹/모바일)에서 "수정 모드"로 드래그해
-- 원하는 순서로 앨범을 재배열할 수 있게 한다. 값이 NULL인 행(수정 모드를
-- 한 번도 쓰지 않은 기존/신규 앨범)은 지금처럼 최신순/가나다순 등 자동
-- 정렬이 그대로 적용되고, 드래그로 순서를 바꾼 순간부터 이 컬럼에 순서가
-- 저장되어 "직접 정렬" 모드에서 그 순서를 그대로 보여준다.
--
-- 실행: Supabase SQL Editor에서 이 파일 전체를 실행

alter table public."USER_VINYL"
  add column if not exists "SORT_ORDER" integer;
